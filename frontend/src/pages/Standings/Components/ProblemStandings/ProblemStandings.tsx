import React from 'react';
import { useStyles } from './ProblemStandingsStyles';


export default function ProblemStandings() : JSX.Element {
    const classes = useStyles();

    return (
        <div></div>
    );
}