import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    codeMirrorContainer: {
        width:'100%',
        backgroundColor: 'white'
    },
});

export { useStyles }