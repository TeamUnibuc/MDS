import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
    container: {
        // margin: '100px 0 0 27%',
        marginTop: '100px',
        display: 'flex',
        alignItems: 'center',
        alignSelf: 'center'
    }
}));

export { useStyles }